# Іспит (90 хв): Міні‑проєкт у Jupyter Notebook

- **Курс**: "Основи генеративного ШІ" (Комп'ютерна інженерія, 2 курс)
- **База курсу**: additional-course-materials/methodological-recommendations (ПР1–ПР7)
- **Формат**: 1 Jupyter Notebook, самостійне виконання в аудиторії
- **Час**: 90 хв

---

## Мета іспиту
- **Перевірити** вміння застосовувати техніки промпт‑інженерії (ПР2, ПР4).
- **Показати** базову роботу з GitHub Models/Azure AI Inference (ПР3, ПР4).
- **Уміти** керувати параметрами генерації та фіксувати результати (температура, токени).
- **Виконати** невелике індивідуальне завдання (варіант) із контролем формату/якості відповіді.

---

## Інструменти і моделі (обов’язково такі самі, як у практичних)
- **SDK/клієнти**: використовуйте ті самі приклади, що у ПР3/ПР4/ПР6/ПР7 (наприклад `azure-ai-inference` і `ChatCompletionsClient`).
- **Моделі**: ті, що використовувались у практичних через GitHub Models/Azure AI Inference (наприклад `gpt-4o-mini` або інш. — залежно від доступності у вашому середовищі).
- **Токени/ключі**: зчитування через `.env` (аналогічно до ПР1–ПР7). Заборонено хардкодити ключі у ноутбуці.

---

## Що потрібно здати
- **Файл**: один `.ipynb` з назвою `Прізвище І_Б_varNN.ipynb` (наприклад: `Петренко І.О._var07.ipynb`).
- **Завантаження**: в Moodle (Завдання: Іспит: Міні‑проєкт із використанням GenAI (Jupyter Notebook)).

---

## Структура ноутбука (рекомендований шаблон)
- **[0] Титул і варіант**
  - ПІБ, група, номер варіанту, обрана модель, endpoint.
- **[1] Підготовка середовища**
  - Імпорти; зчитування `GITHUB_TOKEN` з `.env`; швидкий тест виклику API (1 запит).
- **[2] Базова генерація (10–15 хв)**
  - Базовий промпт під вашу тему. 3 запусків із різними `temperature`/`max_tokens`.
  - Фіксуйте час/токени/якість (короткі нотатки).
- **[2b] Діалоговий режим чат-бота (5–10 хв)**
  - Реалізуйте простий чат у діалоговому режимі з історією повідомлень (`messages`).
  - Використовуйте той самий клієнт/endpoint, що в практичних (GitHub Models/Azure AI Inference, `ChatCompletionsClient` або `requests`).
  - Команди: `/exit` — завершення; `/reset` — очистити історію.
  - Параметри за замовчуванням: `temperature≈0.3`, `max_tokens≈300`. Дозволено змінити.
- **[3] Покращення промпту (15–20 хв)**
  - Додайте 1–2 прийоми з практичних: системний контекст, few‑shot, формат‑контракт (JSON), короткі правила безпеки, post‑перевірка.
  - Повторіть 1–2 запуски; коротко порівняйте з п.2.
- **[4] Індивідуальна частина (15–20 хв)**
  - Виконайте специфічне завдання згідно з вашим варіантом (див. таблицю варіантів).
- **[5] Рефлексія (5 хв)**
  - 5–7 речень: що спрацювало, що ні; які параметри/прийоми дали найбільший ефект; обмеження.

---

## Вимоги до академічної доброчесності
- Використовуйте **ті самі інструменти/моделі**, що у практичних (жодних зовнішніх чат‑ботів). 
- Виведіть у ноутбуці: `os.getenv("GITHUB_TOKEN") is not None`, свій GitHub username та відбиток часу першого запуску.
- Усі промпти/налаштування — власні під час іспиту.

---

## Оцінювання (100 б)

### Детальні критерії:

**[1] Підготовка середовища (10 б)**
- Токен завантажено та перевірено (3 б)
- Клієнт ініціалізовано (azure-ai-inference/requests) (3 б)
- Функція `ask_llm` реалізовано коректно (3 б)
- Sanity-check пройдено (1 б)

**[2] Базова генерація (25 б)**
- Мінімум 3 запити з різними параметрами (10 б)
- Результати зафіксовано у `results_base` (5 б)
- Висновки про вплив параметрів на якість (5 б)
- Аналіз latency/usage (5 б)

**[2b] Діалоговий режим (10 б)**
- Історія повідомлень реалізована (3 б)
- Команди `/exit`, `/reset` працюють (3 б)
- Безпечний цикл з обмеженням ходів (2 б)
- Демонстрація діалогу в виводі (2 б)

**[3] Покращення промпту (30 б)**
- Детальний системний контекст (8 б)
- Few-shot приклади (7 б)
- Формат-контракт (JSON) (7 б)
- Порівняння з базовою генерацією (5 б)
- 2-3 запуски для стабільності (3 б)

**[4] Індивідуальна частина (варіант) (25-30 хв) (30 б)**
- 3+ приклади згенеровано (5 б)
- Валідація формату (JSON schema) (5 б)
- Порівняння результатів (таблиця/візуалізація) (5 б)
- Детальні висновки (5 б)
- Коректність даних перевірено (5 б)
- Стабільність генерації (2 б)
- Використання інструментів з ПР3/ПР4 (3 б)

**[5] Рефлексія (10 б)**
- Аналіз успішних технік (3 б)
- Опис проблем та обмежень (3 б)
- Висновки про найефективніші прийоми (2 б)
- Загальна оцінка виконання (2 б)

### Приклад успішного виконання (Варіант 30 - 87/100 б):

**Сильні сторони:**
- ✅ Середовище налаштовано коректно
- ✅ Діалоговий режим повністю функціональний
- ✅ Системний контекст та JSON-контракт застосовано
- ✅ 3 приклади оцінки згенеровано
- ✅ Рефлексія глибока та вичерпна

**Типові помилки для виправлення:**
- ❌ Відсутні висновки про вплив параметрів (Секція [2])
- ❌ Один тестовий запуск замість 2-3 (Секція [3])
- ❌ Валідація лише через try/except, не jsonschema (Секція [4])
- ❌ Відсутня порівняльна таблиця результатів (Секція [4])
- ❌ Не використано ChatCompletionsClient з ПР3/ПР4
- ❌ Відсутня стабільність генерації (аналіз 2-3 спроб)

**Рекомендовані покращення:**
- Додати `jsonschema` для валідації (як у ПР6)
- Створити pandas DataFrame для порівняння (як у ПР5)
- Використовувати ChatCompletionsClient (як у ПР3/ПР4)
- Зробити 2-3 запити в кожній секції для стабільності
- Писати текстові висновки після кожної секції
- Додати few-shot приклади та системний контекст (як у ПР2/ПР4)

Ключове: коректні виклики API, відтворюваність, короткі висновки, дотримання формату.

---

## Підказки до інструментів (узгоджено з практичними)
- **ПР2**: системний контекст, few‑shot, аналіз токенізації (за бажанням — коротко).
- **ПР3/ПР4**: `ChatCompletionsClient`, параметри `temperature`, `max_tokens`, ролі.
- **ПР6**: формат JSON Schema/контракт, контроль структури відповіді.
- **ПР5/ПР7**: мінімальний контекст або мікро‑RAG (1–2 фрагменти) — лише якщо це прямо потрібно у варіанті.

---

## 30 індивідуальних варіантів
Виконуйте у секції [4]. Оберіть завдання за номером вашого варіанту.

**Стандартна структура кожного варіанту**: генеруйте 3+ приклади → валідуйте формат → порівняйте результати → зробіть висновки.

### Детальний розбір кожного етапу:

#### 🎯 Етап 1: Генеруйте 3+ приклади
**Що робити:**
- Створіть системний промпт з детальними інструкціями для вашої теми
- Додайте few-shot приклад (1-2 зразки правильної JSON структури)
- Використовуйте функцію `ask_llm` з параметрами `temperature=0.4`, `max_tokens=600`
- Згенеруйте **мінімум 3 об'єкти** відповідно до вашого варіанту
- Повторіть генерацію **2-3 рази** для перевірки стабільності результатів

**Приклад для варіанту 1 (Комп'ютерні компоненти):**
```python
system_prompt = "Ви експерт з комп'ютерного обладнання. Генеруйте інформацію у JSON форматі..."
user_prompt = "Опишіть 3 компоненти: CPU, RAM, SSD. Кожен з полів: name, function, speed, cost."
fewshot = '{"components": [{"name": "Intel i7", "function": "...", "speed": "...", "cost": "..."}]}'
```

**Очікуваний результат:**
```json
{
  "components": [
    {"name": "Intel Core i7", "function": "Обробка даних", "speed": "3.5 GHz", "cost": "200-300 USD"},
    {"name": "DDR4 RAM", "function": "Тимчасове зберігання", "speed": "3200 MHz", "cost": "50-80 USD"},
    {"name": "Samsung SSD", "function": "Постійне зберігання", "speed": "550 MB/s", "cost": "80-120 USD"}
  ]
}
```

#### ✅ Етап 2: Валідуйте формат
**Що робити:**
- Створіть JSON схему для вашої структури даних
- Використовуйте бібліотеку `jsonschema` для автоматичної валідації
- Перевірте наявність **всіх обов'язкових полів**
- Перевірте **типи даних** (string, integer, array, object)
- Збережіть результати валідації у змінну

**Приклад валідації:**
```python
import jsonschema

schema = {
    "type": "object",
    "properties": {
        "components": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "name": {"type": "string"},
                    "function": {"type": "string"},
                    "speed": {"type": "string"},
                    "cost": {"type": "string"}
                },
                "required": ["name", "function", "speed", "cost"]
            }
        }
    }
}

# Перевірка валідації
try:
    jsonschema.validate(data, schema)
    print("✅ JSON валідний")
except jsonschema.ValidationError as e:
    print(f"❌ Помилка валідації: {e}")
```

**Очікувані результати валідації:**
- `validation_success = True/False` (загальний результат)
- `valid_objects = 3/3` (кількість коректних об'єктів)
- `missing_fields = []` (список відсутніх полів)
- `type_errors = []` (список помилок типів даних)

#### 📊 Етап 3: Порівняйте результати
**Що робити:**
- Створіть **pandas DataFrame** з усіма згенерованими даними
- Порівняйте ключові характеристики згідно з вашим варіантом
- Побудуйте **таблицю порівняння** або просту візуалізацію
- Проаналізуйте переваги та недоліки кожного варіанту

**Приклад порівняння для варіанту 1:**
```python
import pandas as pd

# Створення DataFrame
df = pd.DataFrame(data["components"])

# Таблиця порівняння
comparison_table = df[["name", "speed", "cost"]].copy()
comparison_table["performance_score"] = df["speed"].apply(calculate_speed_score)
comparison_table["value_score"] = df["cost"].apply(calculate_value_score)

print("📊 Таблиця порівняння компонентів:")
print(comparison_table.to_string(index=False))

# Візуалізація (за бажанням)
import matplotlib.pyplot as plt
plt.figure(figsize=(10, 6))
plt.bar(df["name"], df["cost"].apply(extract_numeric_cost))
plt.title("Порівняння вартості компонентів")
plt.ylabel("Вартість (USD)")
plt.show()
```

**Очікувані результати порівняння:**
- Таблиця з порівняльними характеристиками
- Рейтингові оцінки для кожного об'єкта
- Візуалізація ключових показників
- Аналіз переваг/недоліків

#### 💡 Етап 4: Зробіть висновки
**Що аналізувати:**
- **Стабільність генерації**: відсоток успішних спроб
- **Якість даних**: коректність та повнота інформації
- **Вплив параметрів**: як temperature/max_tokens вплинули на результати
- **Проблеми та обмеження**: що не вдалося, чому
- **Ефективність підходів**: які техніки працювали найкраще

**Приклад висновків:**
```python
# Аналіз стабільності
success_rate = successful_attempts / total_attempts * 100
print(f"📈 Стабільність генерації: {success_rate:.1f}%")

# Порівняння з базовою генерацією
print("🔍 Порівняння з базовою генерацією:")
print(f"- Базова: {base_success_rate}% успішності")
print(f"- Покращена: {success_rate}% успішності")
print(f"- Покращення: {success_rate - base_success_rate:.1f}%")

# Висновки
conclusions = [
    "✅ JSON-контракт значно покращив структуру відповідей",
    "✅ Few-shot приклади допомогли моделі зрозуміти формат",
    "⚠️ Temperature 0.4 оптимальний для балансу стабільності",
    "❌ Іноді модель генерує неповні дані про вартість"
]
```

**Очікувані висновки:**
- Текстовий аналіз результатів (5-7 речень)
- Кількісні показники стабільності
- Порівняння з базовим підходом
- Рекомендації для майбутніх завдань

### 📋 Фінальна структура результатів:
```python
final_results = {
    "variant": 1,
    "attempts": 3,
    "successful": 2,
    "success_rate": 66.7,
    "data": [...],  # згенеровані об'єкти
    "validation": {...},  # результати валідації
    "comparison": {...},  # таблиця порівняння
    "conclusions": [...]  # висновки
}
```

### Блок A: Основи комп'ютерних систем (1-10)
1. **Комп'ютерні компоненти**: Описати 3 компоненти (CPU, RAM, SSD) у JSON `{name, function, speed, cost}`. Порівняти продуктивність.
2. **Операційні системи**: Генерувати 3 ОС (Windows, Linux, macOS) у JSON `{name, interface, hardware_support, use_cases}`.
3. **Файлові системи**: Створити JSON для 3 файлів систем (NTFS, ext4, APFS) `{name, features, max_file_size, compatibility}`.
4. **Мережеві пристрої**: Описати 3 пристрої (router, switch, access point) у JSON `{device, function, speed, range}`.
5. **Принтери та сканери**: Генерувати 3 пристрої (laser, inkjet, 3D printer) у JSON `{type, technology, cost_per_page, quality}`.
6. **Монітори**: Створити JSON для 3 моніторів (LCD, OLED, CRT) `{type, resolution, refresh_rate, power_consumption}`.
7. **Накопичувачі**: Описати 3 типи (HDD, SSD, USB flash) у JSON `{type, capacity, speed, durability}`.
8. **Процесори**: Генерувати 3 CPU (Intel, AMD, ARM) у JSON `{brand, cores, clock_speed, power_usage}`.
9. **Відеокарти**: Створити JSON для 3 GPU (NVIDIA, AMD, Intel) `{brand, vram, cuda_cores, price}`.
10. **Пам'ять**: Описати 3 типи пам'яті (DDR3, DDR4, DDR5) у JSON `{type, speed, capacity, compatibility}`.

### Блок B: Програмне забезпечення та мережі (11-20)
11. **Веб-браузери**: Генерувати 3 браузери (Chrome, Firefox, Safari) у JSON `{name, engine, speed, extensions}`.
12. **Антивіруси**: Створити JSON для 3 антивірусів (Kaspersky, Windows Defender, Avast) `{name, detection_rate, system_impact, price}`.
13. **Офісні пакети**: Описати 3 пакети (MS Office, LibreOffice, Google Docs) у JSON `{suite, features, collaboration, cost}`.
14. **Месенджери**: Генерувати 3 месенджери (Telegram, WhatsApp, Signal) у JSON `{name, encryption, file_size_limit, users}`.
15. **Хмарні сервіси**: Створити JSON для 3 сервісів (Google Drive, Dropbox, OneDrive) `{service, free_space, price, sync_speed}`.
16. **Відеоконференції**: Описати 3 платформи (Zoom, Teams, Google Meet) у JSON `{platform, max_participants, quality, price}`.
17. **Бази даних**: Генерувати 3 типи БД (SQLite, MySQL, MongoDB) у JSON `{type, scalability, query_language, use_cases}`.
18. **Контейнери**: Створити JSON для 3 технологій (Docker, Podman, LXC) `{name, ease_of_use, performance, security}`.
19. **Віртуалізація**: Описати 3 типи (VMware, VirtualBox, Hyper-V) у JSON `{software, performance, features, cost}`.

### Блок C: Інтернет речей та embedded системи (21-30)
21. **Мікроконтролери**: Генерувати 3 MCU (Arduino, ESP32, Raspberry Pi Pico) у JSON `{name, pins, programming_language, cost}`.
22. **Сенсори**: Створити JSON для 3 сенсорів (temperature, humidity, motion) у JSON `{sensor, accuracy, range, price}`.
23. **IoT платформи**: Описати 3 платформи (Arduino IoT, AWS IoT, Azure IoT) у JSON `{platform, protocol, cost, complexity}`.
24. **Бездротові модулі**: Генерувати 3 модулі (WiFi, Bluetooth, LoRa) у JSON `{module, range, power_consumption, price}`.
25. **Актиuatorи**: Створити JSON для 3 виконавчих механізмів (servo, stepper, DC motor) у JSON `{type, precision, power, cost}`.
26. **Розробки плат**: Описати 3 плати (breadboard, PCB, prototype board) у JSON `{type, reusability, cost, complexity}`.
27. **Протоколи передачі даних**: Генерувати 3 протоколи (I2C, SPI, UART) у JSON `{protocol, speed, pins, complexity}`.
28. **Джерела живлення**: Створити JSON для 3 джерел (battery, solar, USB) у JSON `{type, capacity, voltage, cost}`.
29. **Екрани та дисплеї**: Описати 3 типи (LCD, OLED, e-ink) у JSON `{type, power_consumption, color, cost}`.
30. **Корпуси та механіка**: Генерувати 3 матеріали (plastic, metal, 3D printed) у JSON `{material, durability, cost, manufacturing}`.

---

## Таймінг (орієнтир)
- [1] 5–10 хв
- [2] 15–20 хв
- [2b] 10–15 хв
- [3] 20–25 хв
- [4] 25–30 хв
- [5] 5–10 хв

---

## Нотатки щодо середовища
- За потреби використовуйте готові приклади з ноутбуків розділів 06/07/11 (виклик через `ChatCompletionsClient`).
- Усі ключі — лише через `.env`. Не друкуйте значення ключів у вивід.
- За відсутності мережі продемонструйте сформовані промпти, параметри та очікувані формати відповідей.
